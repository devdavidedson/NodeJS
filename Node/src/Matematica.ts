const somar = (x:number, y:number):number => {
    return(x + y)
};

const subtrair = (x:number, y:number):number => {
    return(x - y)
};

const multiplicar = (x:number, y:number):number => {
    return(x * y)
};

export default {
    somar,
    subtrair,
    multiplicar
};